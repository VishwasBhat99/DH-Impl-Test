#[derive(Debug, Clone)]
pub struct CustData {
    pub nob: String,
    pub text_desc: String,
}
#[derive(Debug, Clone)]
pub struct DescMaster {
    pub flag_value: String,
    pub condition: String,
    pub txt_desc_flag: String,
}
#[derive(Debug, Clone)]
pub struct BiuData {
    pub t1: String,
    pub t2: String,
    pub t3: String,
    pub t4: String,
    pub div: String,
}
